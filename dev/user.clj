(ns user
  (:require [integrant.repl :as repl]
            [semantic-forms.core :as core]))


;; (repl/set-prep! (fn [] core/config))

;; (def go repl/go)
;; (def halt repl/halt)
;; (def reset repl/reset)
;; (def reset-all repl/reset-all)

;; (comment
;;   (go)
;;   (halt)
;;   (reset)
;;   (reset-all))
